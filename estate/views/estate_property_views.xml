<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,list,form</field>
        <!--<field name="view_mode">list,form</field>-->
        <field name="context">{'search_default_available': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a property to advertise!
            </p>
        </field>    
    </record>

    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                    decoration-success="state == 'offer_received'" 
                    decoration-bf="state == 'offer_accepted'" 
                    decoration-muted="state == 'sold'">
                <field name="name"/>
                <field name="state" widget="statusbar"/> <!--column_invisible="True"-->
                <field name="property_type_id"/>
                <field name="salesperson_id"/>
                <field name="buyer_id"/>
                <field name="postcode"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
                
                
            </list>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_sold" type="object" string="Sold" 
                            class="btn-primary" 
                            invisible="state in ['sold', 'cancelled']"/>
                    <button name="action_cancel" type="object" string="Cancel" 
                            class="btn-secondary" 
                            invisible="state in ['sold', 'cancelled']"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold" /> <!--statusbar_visible="new,offer_received,offer_accepted,sold"-->
                </header>
                <sheet>
                    <h1>
                        <field name="name" placeholder="Property Name"/>
                    </h1>
                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True, 'no_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability" optional="hide"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ['offer_accepted', 'sold', 'cancelled']">
                                <list editable="bottom" string="Offers">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <field name="status" column_invisible="True"/>
                                    <button name="action_accept" type="object" icon="fa-check" title="Accept Offer" 
                                            invisible="status in ['accepted', 'refused']"/>
                                    <button name="action_refuse" type="object" icon="fa-times" title="Refuse Offer" 
                                            invisible="status in ['accepted', 'refused']"/>
                                </list>
                            </field>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="salesperson_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties">
                <field name="property_type_id"/>
                <field name="salesperson_id"/>
                <field name="buyer_id"/>
                <field name="tag_ids"/>
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <separator/>
                <filter string="Available" name="available" 
                        domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]"/>
                <filter string="Luxury" name="luxury" 
                        domain="[('expected_price', '&gt;', 500000)]"/>
                <filter string="Family Houses" name="family" 
                        domain="[('bedrooms', '&gt;=', 3), ('garden', '=', True)]"/>
                <filter string="Current Month" name="current_month" 
                        domain="[('date_availability', '>=', (context_today() - relativedelta(months=1)).strftime('%Y-%m-01')), 
                                 ('date_availability', '&lt;', (context_today() + relativedelta(months=1)).strftime('%Y-%m-01'))]"/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
    <!-- Kanban View -->
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="property_type_id" records_draggable="false">
                <field name="state"/>
                <field name="property_type_id"/>
                <field name="expected_price"/>
                <field name="best_price"/>
                <field name="selling_price"/>
                <field name="tag_ids"/>
                
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">

                                <div class="o_kanban_record_title">
                                    <field name="name"/>
                                </div>

                                <div class="o_kanban_record_subtitle">
                                    Expected Price: <field name="expected_price"/>
                                </div>

                                <div t-if="record.state.raw_value in ['offer_received', 'offer_accepted']">
                                    Best Offer: <field name="best_price"/>
                                </div>

                                <div t-if="record.state.raw_value in ['offer_accepted', 'sold']">
                                    Selling Price: <field name="selling_price"/>
                                </div>
                                

                                <div class="o_kanban_tags_section">
                                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>

